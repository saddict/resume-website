rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All Firestore access is via the Cloud Function (Admin SDK, bypasses rules).
    // No direct client access is needed or allowed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
